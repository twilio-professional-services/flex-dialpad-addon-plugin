{
  "name": "serverless",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env",
    "deploy": "twilio-run deploy --service_name dialpad-add-on"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
  },
  "dependencies": {
    "twilio-flex-token-validator": "^1.5.1"
  }
}
